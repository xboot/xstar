# ls

列出目录内容。

## 用法

```
ls [-l] [-a] [FILE]...
```

## 选项

| 选项 | 说明 |
|------|------|
| `-l` | 长格式显示：文件类型（`d`=目录、`-`=文件）、权限、大小（字节）、名称 |
| `-a` | 显示所有文件（包括以 `.` 开头的隐藏文件） |

## 说明

- 无参数时列出当前目录
- 默认以多列格式输出（自动适应终端宽度）

## 示例

```bash
# 列出当前目录
ls

# 长格式列出
ls -l

# 列出所有文件
ls -la /kobj/device/
```
