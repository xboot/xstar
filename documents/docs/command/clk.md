# clk

以树状格式导出时钟信息。

## 用法

```
clk [name ...]
```

## 说明

- 无参数时导出整个时钟树层次结构
- 指定名称时只显示指定时钟及其子节点
- 每行显示：名称、频率（MHz，6 位小数）、使能状态（1/0）
- 子节点在其父节点下缩进显示

## 示例

```bash
# 导出完整时钟树
clk

# 查看特定时钟
clk uclk
```
