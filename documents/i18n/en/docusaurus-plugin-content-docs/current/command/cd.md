# cd

Change current working directory.

## Usage

```
cd [dir]
```

## Description

- Without arguments, switches to root directory `/`
- On failure, displays `cd: <path>: No such file or directory`

## Examples

```bash
# Switch to root directory
cd /

# Switch to a subdirectory
cd /device/clk
```
