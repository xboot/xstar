# Geometric Transform (geometric-transform)

## API

```c
struct vision_t * vision_resize(struct vision_t * v, int width, int height);
```

## Notes

Resizes the image to new dimensions and returns a newly allocated Vision image. The original image is unchanged.

